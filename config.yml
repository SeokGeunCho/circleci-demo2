# .circleci/config.yml
version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "Hello circleci"
workflows:
  build-and-test:
    jobs:
      - build
